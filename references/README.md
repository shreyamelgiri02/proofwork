# References

The supplied reference material is preserved unchanged in the parent folder and was not copied (to avoid duplicating ~21 MB of images):

- `../Proofwork-Claude-Build-Prompt.md` — master build prompt (highest priority)
- `../New refrence/` — 19 page mockups (`images/`), page briefs (`prompts/`), `manifest.json`, `README.md`, `SCREEN-INDEX.md`
- `../Old refrence/` — product, business, architecture, data/API, UX, security, workflow, RPC, fixtures, analytics, runbook, research, capacity, AI explanation and checklist specifications (files 00–23)

Conflict priority used: build prompt → new prompts → new mockups → new README/SCREEN-INDEX → old specifications. Notable resolutions are recorded in `CLAUDE_HANDOFF.md`.
